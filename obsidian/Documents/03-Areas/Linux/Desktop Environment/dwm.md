# dwm
dwm is a Window Manager developed by suckless. It is designed to be very minimal with few options available and very recommended if you want to have a simple desktop environment without ricing it very much.
## CONFIGURATION
Unlike i3 or Hyprland, the configuration file for dwm is compiled after each change. It must be downloaded as a folder and compiled with `make`, which substitutes the default configuration with the new one.
